package model.paint.shape;

import java.awt.Point;
import java.awt.geom.AffineTransform;

/**
 * @author zihanli
 * Concrete PolygonFactory that creates fish-shaped Polygons that have an open mouth.
 */
public class Fish1PolygonFactory extends PolygonFactory {

	/**
	 * Constructor that calls the PolygonFactory superclass constructor 
	 * with the scale factor and polygon points that define the fish shape.
	 */
	public Fish1PolygonFactory() {
		super(new AffineTransform(), 0.2, new Point(4, 0), new Point(5, 1), new Point(0, 2), new Point(-4, 0),
				new Point(-5, 2), new Point(-5, -2), new Point(-4, 0), new Point(0, -2), new Point(5, -1));
	}

}
